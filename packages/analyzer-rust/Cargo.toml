[package]
name = "analyzer-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
swc_common = "21.0.1"
swc_ecma_ast = "23.0.0"
swc_ecma_parser = { version = "39.1.0", default-features = false, features = ["typescript"] }

[dev-dependencies]
