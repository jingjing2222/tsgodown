# AOT Emission Ledger

This ledger tracks removal of the generated-Go IR JSON interpreter path.
Generated Go must be typed, ahead-of-time emitted source for supported
semantics. Corpus success does not count if codegen emits
`tsgodownrt.RunProgram("<IR JSON>")` or package-specific shortcuts.

| Key | Area | Contract Status | Go Status | Diagnostic | Evidence | Notes |
|---|---|---|---|---|---|---|
| aot.entry.module | Entry module emission | WIP | WIP | AOT_ENTRY_MODULE_UNSUPPORTED | focused unit tests | Import-free entry module can emit direct `main`; multi-module entry still falls back. |
| aot.module.registry | Module registry | WIP | WIP | AOT_MODULE_REGISTRY_UNSUPPORTED | focused unit tests | Local ESM named function, class, primitive value, and default function import aliases plus simple CJS `module.exports = functionName` / `module.exports = function (...) { ... }` / `exports = module.exports = function (...) { ... }` / `module.exports = ClassName` default `require` imports, `exports.name = functionName` namespace calls, CJS default function static members from `exports.name = value` and `Object.defineProperty(..., { get })`, `createRequire(import.meta.url)` relative package requires already resolved by the Rust module graph, runtime `module.exports.name = value` object writes/reads, object namespace exports such as `const api = { fn }; module.exports = api`, inline `module.exports = { fn }`, re-exported CJS default function namespaces, ESM `export { fn as "module.exports" }` metadata, and JSON/literal CJS default object exports can bind function declarations, basic classes, top-level/default function-expression bindings, and runtime `module.exports` maps to direct Go declarations without `RunProgram`; CJS full cache records and circular evaluation pending. |
| aot.module.init_order | Module init and cache order | WIP | WIP | AOT_MODULE_INIT_ORDER_UNSUPPORTED | focused unit tests | Static CJS require side-effect statements are consumed by the AOT module graph init path without `tsgodownRequire` fallback, and a simple circular CJS mid-export constructor case emits direct Go init functions. Full CJS/ESM evaluation order, mutable cache records, and partially initialized export objects remain pending. |
| aot.function.decl | Function declarations/classes | WIP | WIP | AOT_FUNCTION_DECL_UNSUPPORTED | focused unit tests | Simple numeric, boolean, and string-parameter function declarations plus top-level `const fn = (...) => { return ... }` / `const fn = function () { return ... }` bindings emit direct Go functions. Direct nested function declarations are function-scope hoisted before executable function body statements, and local function parameter requirements propagate into parent function parameter inference. Basic constructor field assignment classes and old-style function constructors used with `new` emit Go structs and constructors; Error/TypeError/RangeError subclasses with constructor `super(...)` and own property assignment lower to direct Go error-map constructors. Closures, full `this`, rest, async, generator pending. |
| aot.function.call | Function calls | WIP | WIP | AOT_FUNCTION_CALL_UNSUPPORTED | focused unit tests | Direct calls to known AOT functions work for numeric, boolean, simple string, and JSON-like object arguments; missing trailing `any` parameters lower to `undefined`/`nil` for default-parameter rewrites; zero-argument known boolean-return calls can feed boolean predicates. Synchronous function-expression IIFEs lower to direct Go closures with inferred parameter slots and lexical captures from the surrounding AOT state. Dynamic call/callable object pending. |
| aot.scope.lexical_slots | Lexical slots | WIP | WIP | AOT_LEXICAL_SLOT_UNSUPPORTED | focused unit tests | Top-level bindings, function params, and function-local typed `var`/`const` slots work for small numeric/string/bool/object subset. TDZ and block scope pending. |
| aot.scope.captured_slots | Captured slots and closures | TODO | TODO | AOT_CAPTURED_SLOT_UNSUPPORTED | planned | Need closure environment structs and mutation semantics. |
| aot.control.if_return | Native if and return control flow | WIP | WIP | AOT_CONTROL_IF_RETURN_UNSUPPORTED | focused unit tests | Top-level `if`, `switch` with `break`, function-local `if` plus `return`, and string-throw `try`/`catch`/`finally` completion subsets emit native Go control flow for supported predicates/discriminants. Caught optional `require(...)` fails through generic Node-shaped errors when inside an observed catch. |
| aot.control.loops | Native loop control flow | WIP | WIP | AOT_LOOP_UNSUPPORTED | focused unit tests | Simple top-level and function-local numeric `for` loops, assignment init into previously undefined numeric locals, numeric `while` with unlabeled `break`/`continue`, and supported string/number/any array `for-of` loops emit native Go control flow. `await` over already-lowered sync expressions inside such loops lowers to the direct expression for non-interleaved subsets. Labeled flow, do-while, and iterator closing pending. |
| aot.expr.numeric | Numeric expressions | WIP | WIP | AOT_NUMERIC_EXPR_UNSUPPORTED | focused unit tests | Numeric literals, arithmetic including exponentiation, bitwise shift/and/or/xor/not for numeric slots, assignment, compound assignment (`+=`, `-=`, `*=`, `/=`, `%=`, `**=`, `&=`, `&#124;=`, `^=`, `<<=`, `>>=`, `>>>=`) for numeric slots, prefix/postfix update, comparisons, strict equality, typed conditional expressions, numeric coercion from typed `any` slots, global `parseInt(...)` subset, `Math.abs(...)`, `Number.prototype.toString(radix)` for integer and finite fractional radix subsets, string `.indexOf` with optional start, string `.charCodeAt`, string `.length`, numeric-array `.length`/index/`.pop()`, and numeric array spread/for-of subsets. JS coercion matrix pending. |
| aot.expr.string | String expressions | WIP | WIP | AOT_STRING_EXPR_UNSUPPORTED | focused unit tests | String literals, interpolated template literals for typed string slots, string parameter inference from concatenation/method calls/templates, `typeof === "string"` branch narrowing, string `+`, string `+=`, string logical-or fallback, typed conditional expressions, `typeof`, global `String(...)` for primitive slots, `String.fromCharCode(...)` for numeric args, dynamic string index reads, `.trim`, `.trimStart`, `.trimEnd`, `.charAt`, `.slice`, `.substring`, `.substr`, `.replace(...)` for string literals and RegExp literals without pattern backreferences, `.replaceAll(...)` string subset, `.repeat`, `.toUpperCase`, `.toLowerCase`, `.match(RegExp)` no-global match-array subset, and string-array `.join` for local slots emit typed Go strings/arrays. Full JS string object/method/coercion semantics pending. |
| aot.expr.boolean | Boolean expressions | WIP | WIP | AOT_BOOLEAN_EXPR_UNSUPPORTED | focused unit tests | Boolean literals, numeric/string/bool/`typeof` comparison predicates, global `Boolean(...)` and `isNaN(...)` subsets for primitive slots, logical-not truthiness for primitive/any slots, logical-and, logical-or, typed conditional expressions, string `.includes`, `.startsWith`, `.endsWith`, and primitive string-array `.includes` for bool subset. Full truthiness/coercion matrix pending. |
| aot.expr.regexp | RegExp expressions | WIP | WIP | AOT_REGEXP_UNSUPPORTED | focused unit tests | RegExp literal `.test(value)` emits Go `regexp.MustCompile(...).MatchString(...)` for patterns whose flags are empty or `i`, including JS `String(...)` coercion for non-literal values. Unsupported flags and broader RegExp APIs remain fail-closed. |
| aot.property.static | Static property access | WIP | WIP | AOT_STATIC_PROPERTY_UNSUPPORTED | focused unit tests | Simple object literal fields emit Go struct fields with direct static property access. Array/string/module property lookup pending. |
| aot.property.dynamic | Dynamic property access | TODO | TODO | AOT_DYNAMIC_PROPERTY_UNSUPPORTED | planned | Plain object map writes/reads include runtime `module.exports.name`, JSON/literal CJS default object reads, nested object member reads, and local object member assignment subsets. Need computed keys, symbol keys, prototype lookup, getters/setters. |
| aot.value.model | Typed Value model | WIP | WIP | AOT_VALUE_MODEL_UNSUPPORTED | focused unit tests | Numeric, string, boolean, symbol, bytes, numeric-array, string-array, mixed `[]any` array literal slots, JSON module arrays/objects, and ordered Map/Set string-key primitive subsets emit typed Go values, including local ESM named value imports; `Symbol(...)`, selected well-known `Symbol.*` values, `typeof Symbol`, `typeof symbol`, direct `symbol.toString()`, and `Symbol.prototype.toString.call(...)` lower through generic symbol helpers; simple object literals emit Go structs; JSON-like object arguments, Error/TypeError/RangeError objects, derived Error subclass objects, and destructuring default temporaries emit `map[string]any` with dynamic property reads, `delete`, and `in` checks for plain object maps; Map `keys()`/`values()` and Set `values()` spread into `[]any` for supported iterator subsets; array `in` handles numeric indexes and `length`; JSON report-shaped array/object/null values emit `[]any`/`map[string]any` plus direct `JSON.stringify` helper; console primitive `null`/`undefined` rendering and nullish equality checks against typed `any` bindings lower for fail-closed function bodies; generic `+` over unknown `any` operands now dispatches through a JS-like add helper instead of forcing string concatenation. Full backend-neutral JS value contract still pending. |
| aot.node.builtins | Node builtin helpers | WIP | WIP | AOT_NODE_BUILTIN_UNSUPPORTED | focused unit tests | Node builtin import specs such as `fs`, `assert`, and `node:crypto` are accepted in the module graph and unused bindings do not force fail-closed; `process.stdout.isTTY` emits a Go helper using `os.Stdout.Stat()`, static `process.env.NAME` reads emit `os.Getenv`, `process.env` object references lower to an environment map, and `process.arch`, `process.chdir`, `process.cwd`, `process.cwd()`, `process.execPath`, `process.getgid()`, `process.getuid()`, `process.version`, `process.versions`, `process.versions.node`, `process.platform`, `process.stdin`, `process.stdout`, `process.stderr`, `process.channel`, process function refs, and direct `process.emitWarning`/`process.on` calls lower through generic Node process helpers for the Node.js 24.15.0 LTS target. `path.basename`, `path.delimiter`, `path.dirname`, `path.isAbsolute`, `path.join`, `path.normalize`, `path.parse`, `path.relative`, `path.resolve`, `path.sep`, `path.posix.sep`, `path.win32.sep`, `os.homedir`, `fs.existsSync`, `fs.statSync` Stats `mode`/`isFile()`/`isDirectory()`/`isSymbolicLink()`, `Buffer.alloc`, `Buffer.from`, `Buffer.isBuffer`, and `assert.equal`/`assert.strictEqual`/`assert.deepStrictEqual` have focused AOT helper coverage for string-path, byte-slice, primitive, and JSON-like object subsets. Broader builtin operations still need generic Node LTS helper lowering and fail closed when observed. |
| aot.async.promise_timer | Promise and timer ordering | TODO | TODO | AOT_ASYNC_UNSUPPORTED | planned | Need direct async contract ops for Promise jobs, nextTick, timers, immediates. |
| aot.diagnostics.fail_closed | Deterministic fail-closed diagnostics | WIP | WIP | AOT_UNSUPPORTED_SEMANTIC | runtime contract tests, focused AOT tests | Product default now fails closed when Go AOT emission cannot render supported IR and reports deterministic AOT feature details for unsupported classes, module bindings, function bodies, unresolved imports, and observed Node builtin operations; builtin diagnostics are scope-aware for params and local bindings so shadowed names such as local `path`/`fs` are not counted as Node APIs. Legacy IR interpreter fallback has been removed from Go emission; unsupported executable semantics cannot opt into an interpreter profile. |
| aot.no_ir_json_interpreter | No IR JSON interpreter in generated Go | WIP | WIP | AOT_IR_JSON_INTERPRETER_FORBIDDEN | focused unit tests, generated small corpus | Simple console/function/control-flow, `console.log`/`console.error`, plus local ESM named/default function/value import, CJS default function export, CJS circular mid-export constructor, createRequire relative package require, CJS JSON module `require`, and Node `assert` builtin import subset cases assert no `RunProgram`; regenerated 10 small corpus generated projects fail closed without `RunProgram` when AOT is incomplete. Go emission no longer contains `render_interpreted_program`, `func RunProgram`, or legacy interpreter runtime source. 21 legacy interpreter-era broad semantics tests are explicitly ignored as pending typed AOT lowering rather than counted as Green. Real corpus AOT parity still pending. |
| aot.holdout.parity | Holdout parity beyond corpus names | TODO | TODO | AOT_HOLDOUT_PARITY_UNSUPPORTED | planned | Need syntax/API holdouts proving no corpus/test-id hardcode. |
| aot.benchmarks | AOT runtime benchmarks | TODO | TODO | AOT_BENCHMARK_UNTRACKED | planned | Need benchmarks proving direct code path, with execa shortcut excluded from score. |

## Gate Rules

- Required rows are enforced by `pnpm run gate:aot-emission-ledger`.
- `TODO` and `WIP` are allowed while developing.
- Final mode must reject `TODO` and `WIP`:

```bash
node scripts/check-ledger.mjs aot-emission --final
```
