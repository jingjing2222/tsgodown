## Summary
- Improve Go scaffold runtime-readiness by emitting robust server bootstrap code:
  - resolve listen address from `TSGODOWN_ADDR` / `PORT` / default `:18081`
  - use `http.Server` with `ReadHeaderTimeout` for safer defaults
- Surface Rust IR diagnostics directly in generated `main.go` as actionable comments (including source location when present).
- Keep SSoT boundary explicit in generated guidance: emitter reflects diagnostics and does not own policy decisions.
- Strengthen smoke checks with an additional runtime-path test (boot + request) guarded by Go toolchain availability.

## TDD
- Added failing tests first for:
  - IR diagnostic comment emission and actionable guidance text.
  - Runtime smoke path (`go run` server boot + `/health` request) when Go is installed.
- Implemented minimal emitter changes to satisfy new tests.

## Validation
- Full gate run passed in order:
  1. `pnpm run lint`
  2. `pnpm run format:check`
  3. `pnpm run guard:runtime`
  4. `pnpm run build`
  5. `pnpm run test`
  6. `cargo fmt --all --check`
  7. `cargo clippy --workspace --all-targets -- -D warnings`
  8. `cargo test --workspace --all-targets`

## Notes
- Runtime smoke tests in `@tsgodown/emitter-go` are safely skipped when Go toolchain is unavailable in local environment.
